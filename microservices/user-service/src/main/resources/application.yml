server:
  port: 8081

spring:
  application:
    name: user-service  # 서비스 이름

  datasource:
    url: jdbc:postgresql://localhost:5432/webnovel_compass
    username: atrafelis
    password: atrafelis
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

# Eureka 클라이언트 설정
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info